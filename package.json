{
	"scripts": {
		"start": "node ./ace nuxtbuild && cross-env NODE_ENV=production node server.js"
		, "dev": "nodemon --watch app --watch bootstrap --watch config --watch .env -x node server.js"
	}
	, "dependencies": {
		"adonis-ace": "3.x"
		, "adonis-auth": "1.x"
		, "adonis-commands": "2.x"
		, "adonis-fold": "3.x"
		, "adonis-framework": "3.x"
		, "adonis-lucid": "3.x"
		, "adonis-middleware": "1.x"
		, "adonis-websocket": "1.x"

		, "nuxt": "latest"

		, "ccxt": "1.x"

		, "youch": "2.x"
	}
	, "autoload": {
		"App": "./app"
	}
	, "devDependencies": {
		"cross-env": "5.x"
		, "nodemon": "1.x"
	}
}